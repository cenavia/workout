## Ticket: WT-001

**Título:** Implementar endpoint de registro de usuario
**Tipo:** [FEATURE]
**Estimación:** 2h
**Prioridad:** Crítica
**Asignado a:** Sin asignar
**Sprint:** Sprint 1

---

### Descripción

Implementar el endpoint de registro de usuario que permite crear nuevas cuentas en la plataforma. El endpoint debe validar los datos de entrada, verificar que el email no esté duplicado, hashear la contraseña de forma segura y almacenar el usuario en la base de datos.

### User Story Relacionada

**Como** usuario nuevo  
**Quiero** registrarme en la plataforma con email y contraseña  
**Para** poder acceder a todas las funcionalidades de Workout Tracker

---

### Criterios de Aceptación

- [ ] Endpoint `POST /api/auth/register` implementado
- [ ] Validación de email con regex (formato válido)
- [ ] Validación de contraseña (mínimo 8 caracteres)
- [ ] Validación de que el email no esté ya registrado
- [ ] Hash de contraseña con bcrypt (salt rounds: 10)
- [ ] Almacenamiento seguro en base de datos
- [ ] Respuesta exitosa con mensaje de confirmación
- [ ] Manejo de errores: 400 para datos inválidos, 409 para email duplicado
- [ ] Tests unitarios implementados
- [ ] Documentación OpenAPI actualizada

---

### Especificación Técnica

#### Endpoint

| Método | URL | Descripción |
|--------|-----|-------------|
| POST | `/api/auth/register` | Registrar nuevo usuario |

#### Request Body

```json
{
  "email": "usuario@example.com",
  "password": "password123",
  "name": "Juan Pérez"
}
```

#### Campos/Modelo

| Campo | Tipo | Requerido | Validación | Descripción |
|-------|------|-----------|------------|-------------|
| email | String | Sí | Email válido, único | Email del usuario |
| password | String | Sí | Mínimo 8 caracteres | Contraseña (se hashea) |
| name | String | Sí | Mínimo 2 caracteres | Nombre completo |

#### Response

**200 OK:**
```json
{
  "message": "Usuario registrado exitosamente",
  "user": {
    "id": "uuid",
    "email": "usuario@example.com",
    "name": "Juan Pérez"
  }
}
```

**400 Bad Request:**
```json
{
  "error": "Datos inválidos",
  "details": ["El email no es válido", "La contraseña debe tener al menos 8 caracteres"]
}
```

**409 Conflict:**
```json
{
  "error": "El email ya está registrado"
}
```

#### Archivos a Modificar

**Backend:**
- `workout-core/src/contexts/auth/application/register-user.use-case.ts` - Caso de uso
- `workout-core/src/contexts/auth/infrastructure/http/auth.router.ts` - Endpoint HTTP
- `workout-core/src/contexts/auth/domain/user.entity.ts` - Entidad de dominio
- `workout-core/src/contexts/auth/infrastructure/repositories/user.repository.ts` - Repositorio
- `workout-core/src/contexts/auth/application/dtos/register-user.dto.ts` - DTOs

**Tests:**
- `workout-core/src/contexts/auth/application/register-user.use-case.test.ts`
- `workout-core/src/contexts/auth/infrastructure/http/auth.router.test.ts`

---

### Tareas Técnicas

1. [ ] Crear DTO de registro con validaciones
2. [ ] Implementar caso de uso `RegisterUserUseCase`
3. [ ] Implementar método de repositorio para verificar email existente
4. [ ] Implementar hash de contraseña con bcrypt
5. [ ] Crear endpoint HTTP con validación de entrada
6. [ ] Implementar manejo de errores (400, 409)
7. [ ] Escribir tests unitarios para caso de uso
8. [ ] Escribir tests de integración para endpoint
9. [ ] Actualizar documentación OpenAPI/Swagger
10. [ ] Code review

---

### Dependencias

- **Bloqueado por:** Ninguno
- **Bloquea a:** WT-002 (Login), WT-004 (JWT Protection)
- **Relacionado con:** WT-002, WT-003

---

### Requisitos No Funcionales

- **Seguridad:** 
  - Contraseñas nunca se almacenan en texto plano
  - Hash con bcrypt (salt rounds: 10)
  - Validación de entrada para prevenir inyección
- **Performance:** 
  - Verificación de email duplicado debe usar índice en BD
  - Respuesta en < 200ms para casos exitosos
- **Accesibilidad:** N/A (backend)

---

### Notas Adicionales

- Usar librería de validación (Zod, Joi o similar) para validar entrada
- El hash de contraseña debe hacerse antes de almacenar en BD
- Considerar rate limiting para prevenir abuso del endpoint
- El email debe normalizarse (lowercase, trim) antes de validar
- Revisar estándares de backend en `ai-specs/specs/backend-standards.mdc`

---

### Especificación BDD

```gherkin
Feature: Registro de Usuario
  Como usuario nuevo
  Quiero registrarme en la plataforma con email y contraseña
  Para poder acceder a todas las funcionalidades de Workout Tracker

  Scenario: Registro exitoso con datos válidos
    Given que no existe un usuario con email "nuevo@example.com"
    When envío POST /api/auth/register con email, password y name válidos
    Then recibo respuesta 200 con mensaje de éxito
    And el usuario se crea en la base de datos con password hasheado
    And el email se almacena en lowercase

  Scenario: Error al registrar con email duplicado
    Given que existe un usuario con email "existente@example.com"
    When envío POST /api/auth/register con email "existente@example.com"
    Then recibo respuesta 409 Conflict
    And el mensaje indica que el email ya está registrado

  Scenario: Error al registrar con contraseña corta
    When envío POST /api/auth/register con password de 6 caracteres
    Then recibo respuesta 400 Bad Request
    And el mensaje indica que la contraseña debe tener al menos 8 caracteres

  Scenario: Error al registrar con email inválido
    When envío POST /api/auth/register con email "email-invalido"
    Then recibo respuesta 400 Bad Request
    And el mensaje indica que el email no es válido
```
